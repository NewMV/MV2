name: TradingView Scraper – Test (20 Symbols)

on:
  workflow_dispatch:

jobs:
  scraper-test:
    runs-on: ubuntu-latest

    env:
      SHARD_INDEX: 0
      SHARD_STEP: 1
      START_INDEX: 1
      END_INDEX: 20
      CHECKPOINT_FILE: checkpoint_test.txt

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      # ---- GOOGLE SHEETS CREDENTIALS ----
      - name: Create credentials.json
        run: |
          cat << 'EOF' > credentials.json
          ${{ secrets.GSPREAD_CREDENTIALS }}
          EOF

      # ---- TRADINGVIEW LOGIN COOKIES ----
      - name: Create cookies.json
        run: |
          cat << 'EOF' > cookies.json
          ${{ secrets.TRADINGVIEW_COOKIES }}
          EOF

      # ---- VALIDATE FILES (FAIL FAST) ----
      - name: Validate JSON files
        run: |
          pytho
